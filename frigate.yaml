mqtt:
  enabled: false

detectors:
  coral:
    type: edgetpu
    device: usb

# ffmpeg:
#   hwaccel_args: preset-libmfx

cameras:
  back_door:
    enabled: true
    ffmpeg:
      inputs:
        - path: rtsps://192.168.1.212:7441/qoYpzimaZgXlUhzc?enableSrtp
          roles:
            - detect
    detect:
      enabled: true
      width: 1280
      height: 720

version: 0.15-1
