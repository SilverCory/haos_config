mqtt:
  enabled: false

detectors:
  coral:
    type: edgetpu
    device: usb

cameras:
  back_door: # <------ Name the camera
    enabled: true
    ffmpeg:
      inputs:
        - path: rtsps://192.168.1.212:7441/IKaf72QSI3G17ptF?enableSrtp
          roles:
            - detect
    detect:
      enabled: false # <---- disable detection until you have a working camera feed
      width: 1280
      height: 720
version: 0.15-1
